#pragma once

#include <chrono>

using namespace std::chrono_literals;

namespace ByBit {
    namespace websocket {
        constexpr auto host = "";
        constexpr auto port = "";
        constexpr auto auth_timeout = 5s;
        constexpr auto url = "";
        constexpr auto reconnect_delay = 2s;
    }

    constexpr auto api_key = "";
}
